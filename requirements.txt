scenedetect==0.7
ultralytics==8.4.46
tqdm==4.67.3
# Security: pinned (audit finding P2-2) -- an unpinned yt-dlp let CI/deploys
# silently pull in whatever the latest release happens to be, which is both
# a supply-chain risk and a reproducibility problem. yt-dlp ships frequent
# releases to keep up with site changes, so this pin should be bumped
# deliberately and re-tested rather than left to float.
yt-dlp==2026.8.19
bgutil-ytdlp-pot-provider
faster-whisper==1.2.1
google-genai==1.75.0
python-dotenv==1.2.2
# Security: bumped from 0.10.14 (CVE re-audit) -- purely to drop mediapipe's
# own `protobuf<5` constraint (see the explicit protobuf pin below).
# mediapipe.python.solutions (mp.solutions.face_detection/face_mesh) was
# REMOVED starting 0.10.30, not just deprecated -- main.py has since been
# ported to mediapipe.tasks.python.vision (FaceDetector/FaceLandmarker, see
# _ensure_mediapipe_model and detect_face_candidates/_track_mouth_signals).
#
# Pinned to 1.0.1, NOT 0.10.35: PyPI has no linux/aarch64 wheel for
# mediapipe between 0.10.18 and 1.0.0 (confirmed by a real
# `docker compose up --build` failure on an Apple Silicon Mac --
# "Could not find a version that satisfies the requirement
# mediapipe==0.10.35" -- x86_64 had a 0.10.35 wheel, which is why that
# gap wasn't visible in this environment's own x86_64 sandbox testing).
# 1.0.1 has the same Tasks API surface as 0.10.35 (no mp.solutions in
# either) and the exact same ported main.py code was re-verified
# end-to-end against it with identical results.
mediapipe==1.0.1
# Security: mediapipe<0.10.30ish (pre-Tasks-API-only releases, including
# the 0.10.14 this was pinned to before) transitively forced
# protobuf<5,>=4.25.3, pinning it to the vulnerable 4.25.9 (CVE re-audit,
# pip-audit: PYSEC-2026-1805). Newer mediapipe no longer declares a
# protobuf dependency at all, so pin it here explicitly at a fixed version
# clear of the known CVEs.
protobuf==5.29.6
boto3==1.43.4

fastapi==0.136.1
uvicorn==0.46.0
# Security: bumped from 0.0.27 (CVE re-audit, pip-audit) -- this package
# parses every multipart/form-data upload this app accepts, i.e. it sits
# directly in the path of attacker-controlled input on every upload endpoint.
python-multipart==0.0.31
httpx==0.28.1
# Security: bumped from 12.2.0 (CVE re-audit, pip-audit).
Pillow==12.3.0
beautifulsoup4==4.14.3

# Security: pinned exact versions instead of open-ended '>=' ranges (audit
# finding P2-2) -- unbounded ranges mean a future release (including a
# compromised/malicious one) gets installed automatically on the next build.
assemblyai==0.65.0
openai==1.109.1
pytest==9.1.1
supabase==2.31.0
stripe==15.6.1
itsdangerous==2.2.0
sib-api-v3-sdk==7.6.0
# PyJWT must satisfy supabase-auth's own pin (>=2.12.0 as of supabase 2.31.0)
# -- bumped from 2.10.1 after pinning supabase to an exact version caused a
# pip dependency-resolution conflict (ResolutionImpossible) at build time.
PyJWT==2.13.0
# Security: bumped from 41.0.7 (CVE re-audit, pip-audit) -- this is the
# library behind the AES-GCM OAuth token encryption (see AESGCM/HKDF usage
# in app.py), so keeping it current matters more than most dependencies
# here. 49.0.0 cleared every CVE flagged against 41.0.7, but pip-audit then
# surfaced PYSEC-2026-3552 against 49.0.0 itself (fixed in 50.0.0), so this
# is pinned to the latest release instead of the bare minimum. Only
# cryptography.hazmat.primitives (AESGCM, HKDF, hashes) is used, which has
# been a stable surface across this whole version range.
cryptography==50.0.1
# Async file I/O for upload/download paths inside async def endpoints
# (audit: SonarQube python:S7493 -- avoid blocking the event loop with
# synchronous open()/read()/write() in async request handlers).
aiofiles==25.1.0
